{
  "name": "@sprang/mcp",
  "version": "0.2.1",
  "type": "module",
  "license": "MIT",
  "bin": {
    "sprang-mcp": "./dist/server.js"
  },
  "scripts": {
    "build": "tsup src/server.ts --format esm --dts --sourcemap",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsx src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.3",
    "@sprang/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.3.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.8"
  }
}
